# Core LangChain (use these package names — not the legacy monorepo imports)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0

# Vector database
chromadb>=0.5.0

# Embeddings — install CPU-only torch first (see README setup guide)
sentence-transformers>=3.0.0
torch>=2.3.0

# Document loaders
pypdf>=4.3.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Frontend
streamlit>=1.38.0

# Analytics dashboard (data wrangling for the usage analytics page)
pandas>=2.0.0

# Configuration & validation
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# HTTP client used by Streamlit to call FastAPI
requests>=2.32.0
httpx>=0.27.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
